# V10 starts with a deterministic local simulation. No credentials are needed.
TRANSIT_REALTIME_MODE=simulated

# Future authorized operator/partner feed configuration.
# TRANSIT_REALTIME_MODE=live
# TRANSIT_REALTIME_URL=
# TRANSIT_REALTIME_TOKEN=
# TRANSIT_REALTIME_FORMAT=json
